# Sin NEXT_PUBLIC_API_URL el front usa /api/proxy (route interno) → FastAPI en 127.0.0.1:8000. Destino del proxy:
# BERU_API_PROXY_TARGET=http://127.0.0.1:8000

# Llamada directa del navegador al API (p. ej. producción):
# NEXT_PUBLIC_API_URL=https://api.rentaequip.com
